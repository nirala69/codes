#include<stdio.h>
int main()
{
    int a,b;
    printf("enter distance between cities");
    scanf("%d",&a);

    b=1000*a;//meter
    printf("distance in meter is=%d\n",b);
    return 0;
}
